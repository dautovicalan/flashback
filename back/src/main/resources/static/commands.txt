docker run \
    -p 9090:9090 \
    --name prometheus_flashback \
    -v ./prometheus.yml:/etc/prometheus/prometheus.yml \
    prom/prometheus


 http://localhost:8080/actuator/metrics

 Containers to start
 docker compose up -d - in the root of the this project
 grafana - admin/admin

 npm start dev
 postgres flashback
 keycloak
 es01
 kib01